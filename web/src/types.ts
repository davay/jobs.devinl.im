export interface JobDTO {
  title: string
  company: string
  date: string
  retrieval_date: string
  url: string
}

export interface SourceDTO {
  company_name: string
  category_id: number
  category_name: string
  url: string
}

